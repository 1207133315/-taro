const testPath = "/test"
const loginPath = "/login"// 登录页
const dashboardPath = "/dashboard"// 内容页
const dashboardCategoryPath = "/dashboard/category"//内容中的类型页面
const dashboardArticlePath = "/dashboard/article"//内容中的文章页面
const detailPath = "/detail"//内容中的文章页面
const homePath = "/"// 首页

export default {
  testPath, loginPath, dashboardPath, dashboardCategoryPath, dashboardArticlePath, detailPath,homePath
}
